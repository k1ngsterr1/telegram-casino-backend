# Документация Aviator WebSocket API

Полная документация по WebSocket API для игры Aviator, включая состояния, события, валидацию и обработку ошибок.

---

## 📚 Документы

### Основные документы

#### 1. [AVIATOR_WEBSOCKET_STATES.md](./AVIATOR_WEBSOCKET_STATES.md)

**Полная документация WebSocket API**

- 17 событий от сервера
- 10 событий от клиента
- Структуры данных и примеры
- Жизненный цикл игры
- Формулы расчета множителя
- Provably Fair верификация

**Для кого:** Разработчики фронтенда, интегрирующие WebSocket

---

#### 2. [AVIATOR_QUICK_REFERENCE.md](./AVIATOR_QUICK_REFERENCE.md)

**Быстрая памятка по API**

- Краткое описание всех событий
- Структуры payload
- Чек-лист интеграции
- Типичные ошибки

**Для кого:** Быстрый справочник для опытных разработчиков

---

#### 3. [AVIATOR_BET_VALIDATION.md](./AVIATOR_BET_VALIDATION.md)

**Система валидации ставок**

- 7 проверок на сервере
- Валидация баланса (КРИТИЧНО!)
- Примеры React компонентов
- Обработка ошибок на клиенте
- CSS стили форм
- Тест-кейсы

**Для кого:** Разработчики форм ставок

---

#### 4. [AVIATOR_ERROR_CODES.md](./AVIATOR_ERROR_CODES.md)

**Коды ошибок и их обработка**

- Таблица всех ошибок
- HTTP статус коды
- Быстрые проверки на клиенте
- Примеры обработчиков

**Для кого:** Отладка и обработка ошибок

---

### Проблемы и решения

#### 5. [FIX_MYBET_ID_NULL.md](./FIX_MYBET_ID_NULL.md)

**Решение: myBetId = null**

- Причина проблемы
- Полное решение с кодом
- React хуки
- Чек-лист исправления
- Типичные ошибки

**Для кого:** Если кешаут не работает

---

#### 6. [FIX_STUCK_AVIATOR_GAME.md](./FIX_STUCK_AVIATOR_GAME.md)

**Решение: застрявшая игра**

- SQL скрипты сброса
- Проверка игрового цикла
- Логирование
- Мониторинг

**Для кого:** Если игра не переходит из WAITING в ACTIVE

---

#### 7. [AVIATOR_ERROR_HANDLING.md](./AVIATOR_ERROR_HANDLING.md)

**Обработка ошибок в WebSocket**

- Try-catch блоки
- Graceful degradation
- Логирование ошибок
- Recovery strategies

**Для кого:** DevOps и backend разработчики

---

### Дополнительно

#### 8. [AVIATOR_HISTORY_EXAMPLE.md](./AVIATOR_HISTORY_EXAMPLE.md)

**Примеры работы с историей игр**

- API истории
- Статистика и аналитика
- График множителей
- React компоненты
- Поиск паттернов

**Для кого:** Разработчики статистики и аналитики

---

#### 9. [PROVABLY_FAIR.md](./PROVABLY_FAIR.md)

**Алгоритм Provably Fair**

- HMAC-SHA256
- Генерация множителя
- Верификация результатов
- Серверный и клиентский сиды

**Для кого:** Понимание честности игры

---

## 🚀 Быстрый старт

### 1. Подключение к WebSocket

```typescript
import { io } from 'socket.io-client';

const socket = io('wss://your-domain/ws', {
  auth: {
    token: 'your-jwt-token',
  },
});

socket.on('connected', (data) => {
  console.log('✅ Connected:', data.activeUsers);
});
```

### 2. Получение текущей игры

```typescript
socket.emit('aviator:getCurrent');

socket.on('aviator:game', (game) => {
  console.log('Current game:', game);
  // game.status: WAITING | ACTIVE | FINISHED
});
```

### 3. Размещение ставки

```typescript
socket.emit('aviator:placeBet', {
  aviatorId: game.id,
  amount: 100,
});

socket.on('aviator:betPlaced', (data) => {
  console.log('✅ Bet placed:', data.id);
  setMyBetId(data.id); // ВАЖНО!
});
```

### 4. Кешаут

```typescript
socket.emit('aviator:cashOut', {
  betId: myBetId,
  currentMultiplier: 2.5,
});

socket.on('aviator:cashedOut', (result) => {
  console.log('💰 Won:', result.winAmount);
});
```

---

## 📊 События WebSocket

### От сервера (Server → Client)

1. `connected` - Подключение установлено
2. `aviator:game` - Информация об игре
3. `aviator:statusChange` - Смена статуса
4. `aviator:countdown` - Обратный отсчет
5. `aviator:crashed` - Самолет крашнулся
6. `aviator:crashHistory` - История крашей
7. `aviator:win` - Вы выиграли (персонально)
8. `aviator:lose` - Вы проиграли (персонально)
9. `aviator:betPlaced` - Ставка размещена (персонально)
10. `aviator:newBet` - Кто-то сделал ставку (всем)
11. `aviator:cashOut` - Кто-то сделал кешаут (всем)
12. `aviator:cashedOut` - Кешаут успешен (персонально)
13. `aviator:history` - История игр
14. `activeUsersCount` - Количество онлайн
15. `error` - Ошибка

### От клиента (Client → Server)

1. `aviator:getCurrent` - Получить текущую игру
2. `aviator:getHistory` - Получить историю
3. `aviator:placeBet` - Разместить ставку
4. `aviator:cashOut` - Сделать кешаут
5. `ping` - Проверка соединения

---

## ⚠️ Критичные моменты

### 1. Сохранение ID ставки

```typescript
// ❌ НЕПРАВИЛЬНО
socket.emit('aviator:placeBet', { ... });
// Не сохранили ID!

// ✅ ПРАВИЛЬНО
socket.on('aviator:betPlaced', (data) => {
  setMyBetId(data.id); // Сохранить!
});
```

### 2. Проверка баланса

```typescript
// Всегда проверяйте перед ставкой
if (userBalance < betAmount) {
  showError('Insufficient balance');
  return;
}
```

### 3. Обработка ошибок

```typescript
socket.on('error', (error) => {
  console.error('Error:', error.message);
  // Показать пользователю
});
```

---

## 🔧 Настройки по умолчанию

```typescript
{
  minBet: 25,
  maxBet: 10000,
  minMultiplier: 1.0,
  maxMultiplier: 100000,
  targetRtp: 0.89,
  instantCrashP: 0.01
}
```

---

## 📈 Жизненный цикл игры

```
WAITING (5 сек)
  ↓
ACTIVE (полет)
  ↓
FINISHED (краш)
  ↓
Пауза 3 сек
  ↓
WAITING (новая игра)
```

---

## 🎯 Чек-лист интеграции

- [ ] WebSocket подключение с JWT токеном
- [ ] Слушаем `aviator:game` для состояния игры
- [ ] Слушаем `aviator:betPlaced` и сохраняем ID
- [ ] Проверяем баланс перед ставкой
- [ ] Проверяем `myBetId !== null` перед кешаутом
- [ ] Обрабатываем событие `error`
- [ ] Сбрасываем `myBetId` после win/lose
- [ ] Показываем обратный отсчет (`aviator:countdown`)
- [ ] Обновляем баланс после операций

---

## 🐛 Частые проблемы

### Проблема: Не могу сделать кешаут

**Причина:** `myBetId = null`

**Решение:** См. [FIX_MYBET_ID_NULL.md](./FIX_MYBET_ID_NULL.md)

---

### Проблема: Игра застряла в WAITING

**Причина:** Игровой цикл не работает

**Решение:** См. [FIX_STUCK_AVIATOR_GAME.md](./FIX_STUCK_AVIATOR_GAME.md)

---

### Проблема: Ставка не проходит

**Причина:** Недостаточный баланс или другие проверки

**Решение:** См. [AVIATOR_BET_VALIDATION.md](./AVIATOR_BET_VALIDATION.md)

---

## 💡 Примеры кода

### React Hook для Aviator

```typescript
export function useAviator() {
  const [socket, setSocket] = useState<Socket | null>(null);
  const [myBetId, setMyBetId] = useState<number | null>(null);
  const [hasBet, setHasBet] = useState(false);
  const [gameStatus, setGameStatus] = useState('WAITING');

  useEffect(() => {
    const newSocket = io('wss://your-domain/ws', {
      auth: { token: getToken() },
    });

    newSocket.on('aviator:betPlaced', (data) => {
      setMyBetId(data.id);
      setHasBet(true);
    });

    newSocket.on('aviator:game', (game) => {
      setGameStatus(game.status);
    });

    setSocket(newSocket);
    return () => newSocket.close();
  }, []);

  return { socket, myBetId, hasBet, gameStatus };
}
```

---

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте соответствующий документ из списка выше
2. Посмотрите логи браузера и сервера
3. Убедитесь что используете последнюю версию API
4. Проверьте JWT токен и CORS настройки

---

## 🔄 Версионирование

- **Текущая версия:** 1.0
- **Endpoint:** `/ws`
- **Протокол:** Socket.IO
- **Аутентификация:** JWT в handshake.auth.token

---

## 📝 Changelog

### v1.0 (2025-11-23)

- Полная документация WebSocket API
- Система валидации ставок
- История игр
- Обработка ошибок
- Примеры React компонентов

---

## 🤝 Контрибьюция

При обновлении документации:

1. Обновите соответствующий MD файл
2. Обновите этот README если добавлены новые документы
3. Добавьте примеры кода где необходимо
4. Обновите версию и changelog

---

## 📄 Лицензия

© 2025 Casino Project. Все права защищены.
